import { Box } from "@chakra-ui/layout";

const SideBar = () => {

  return (
    <Box h={"100vh"} p={5}>
      {/* <GenreList
        onSelectGenre={(genre) => {
          setSelectedGenre(genre);
          console.log(genre);
          
        }}
      /> */}
    </Box>
  );
};

export default SideBar;
